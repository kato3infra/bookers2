<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/982eff1ad1.js" crossorigin="anonymous"></script>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container d-flex justify-content-between align-items-center">
          <a class="navbar-brand" href="/">
            <span>Bookers</span>
          </a>
          <button class="navbar-toggler ml-auto" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
                        aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
              <% if user_signed_in? %>
                <li class="nav-item">
                  <a class="nav-link text-light d-flex align-items-center" href="<%= user_path(current_user) %>">
                    <i class="fa-solid fa-house"></i> Home
                  </a>
                </li>
                <li class="nav-item">
                  <a class="nav-link text-light d-flex align-items-center" href="<%= users_path %>">
                    <i class="fa-solid fa-users"></i> Users
                  </a>
                </li>
                <li class="nav-item">
                  <a class="nav-link text-light d-flex align-items-center" href="<%= books_path %>">
                    <i class="fa-solid fa-book-open"></i> Books
                  </a>
                </li>
                <li class="nav-item">
                  <a class="nav-link text-light d-flex align-items-center" href="<%= destroy_user_session_path %>" data-method="delete">
                    <i class="fa-solid fa-right-from-bracket"></i> Log out
                  </a>
                </li>
              <% else %>
                <li class="nav-item">
                  <a class="nav-link text-light d-flex align-items-center" href="<%= root_path %>">
                    <i class="fa-solid fa-house"></i> Home
                  </a>
                </li>
                <li class="nav-item">
                  <a class="nav-link text-light d-flex align-items-center" href="<%= about_path %>">
                    <i class="fa-solid fa-link"></i> About
                  </a>
                </li>
                <li class="nav-item">
                  <a class="nav-link text-light d-flex align-items-center" href="<%= new_user_registration_path %>">
                    <i class="fa-solid fa-user-plus"></i> Sign up
                  </a>
                </li>
                <li class="nav-item">
                  <a class="nav-link text-light d-flex align-items-center" href="<%= new_user_session_path %>">
                    <i class="fa-solid fa-right-to-bracket"></i> Log in
                  </a>
                </li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>

    <main class="mb-auto">
      <% if flash[:notice] %>
        <div class="alert alert-success">
          <p id="notice"><%= flash[:notice] %></p>
        </div>
      <% elsif flash[:alert] %>
        <div class="alert alert-danger">
          <p id="alert"><%= flash[:alert] %></p>
        </div>
      <% end %>
      <%= yield %>
    </main>

    <footer>
      <div class="container mt-5">
        <div class="row">
          <div class="mx-auto text-center">
            <p>CopyRight Infratop.inc</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
